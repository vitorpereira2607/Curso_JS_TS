/*
    Comparison Operators
        >  bigger then
        >= bigger then or equal
        <  less than
        <= less than or equal
        == equality (compares only value between two elements)
        === strict equality (compares value and type between two elements)
        != different (Inequality)
        !== strict inequality
*/