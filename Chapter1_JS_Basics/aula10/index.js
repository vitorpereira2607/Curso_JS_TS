/* 
    Primitive data types
        -String
        -Number
        -Boolean
        -Undefined
        -Null
*/ 

const name = 'Vitor'; //string
const name1 = "Vitor"; //string
const name2 = `Vitor`; //string
const num1 = 'Vitor'; //number
const num2 = 'Vitor'; //number
let varUndefined; //undefined -> does not point to any location in memory
const varNull = null; // NULL -> does not point to any location in memory

const studentApproved = true; //boolean types always return true or false by logic

console.log(typeof studentApproved, studentApproved)

